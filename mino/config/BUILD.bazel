package(default_visibility = ["//visibility:public"])

cc_library(
    name = "deployment_config",
    srcs = ["deployment_config.cc"],
    hdrs = ["deployment_config.h"],
    # toml++ 3.4's single header uses a pre-C++23 literal spelling.
    copts = ["-Wno-deprecated-literal-operator"],
    deps = [
        "//mino/common:result",
        "//mino/common:status",
        "@tomlplusplus",
    ],
)

cc_test(
    name = "deployment_config_test",
    srcs = ["deployment_config_test.cc"],
    data = [
        "//configs:node.production-edge.toml",
        "//configs:security-domains.toml",
    ],
    deps = [
        ":deployment_config",
        "//mino/common:status",
        "@googletest//:gtest_main",
    ],
)

cc_library(
    name = "logging_config",
    srcs = [
        "logging_config.cc",
        "toml_config.cc",
    ],
    hdrs = [
        "logging_config.h",
        "toml_config.h",
    ],
    # toml++ 3.4's generated single header uses the pre-C++23 spaced literal
    # operator spelling, which newer Clang deprecates under the project-wide
    # -Werror policy.
    copts = ["-Wno-deprecated-literal-operator"],
    deps = [
        "//mino/common:result",
        "//mino/common:status",
        "//mino/common/logging",
        "//mino/common/logging:logger",
        "//mino/common/logging:spdlog_backend",
        "@tomlplusplus",
    ],
)

cc_test(
    name = "logging_config_test",
    srcs = ["logging_config_test.cc"],
    deps = [
        ":logging_config",
        "//mino/common:status",
        "@googletest//:gtest_main",
    ],
)
